extends ../component/layout/template
block cool-config
    -
        var myTitle='Learn to code HTML';
        var myDescription='HTML is easy to learn!';
        var landingPage='HTML';
        var activeLink='';


block awesome-content
    h1=myTitle
    p=myDescription
    h2 HTML Attributes
    p Attributes provide additional information about HTML elements.
    h2 HTML Comments
    p Comment tags are used to insert comments in the HTML source code.
    h2 HTML Headings
    p Headings are defined with the h1 to h6 tags.h1 defines the most important heading. h6 defines the least important heading.